# 2013 draftees

Status: NOT STARTED. The simulation's own actual selections once the draft is run. Never a real 2013 selection or destination (Document 2 §12) — the simulation's own pick inventory derives from its own standings, not the real order.
